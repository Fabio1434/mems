# Moteur IA (ai_engine.py)
scikit-learn>=1.3
numpy>=1.24
pandas>=2.0
pyyaml>=6.0
joblib>=1.3

# Benchmarks (scripts/plot_benchmark.py)
matplotlib>=3.7

# Tests (tests/test_ai_engine_logic.py)
pytest>=7.4

# NB: bcc (accès aux BPF Maps) n'est PAS installable via pip -- il doit être
# installé au niveau système : apt-get install -y bpfcc-tools python3-bpfcc
# (déjà automatisé dans lab/topology.clab.yaml)
